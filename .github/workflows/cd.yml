# name: CD Pipleline
# runs-name: ${{ github.actor }}
# on:
#   workflow_run: 
#     workflows:
#       - CI Pipeline # Triggered when CI Pipeline is completed 
#     types: 
#       - completed 
#     branches: ['main','master']

# jobs: 
#   deploy-backend: 
#     name: Deploy Backend 
#     runs-on: ubuntu-latest
#     if: ${{ github.event.workflow_run.conclusion == 'success' }} # Only runs if CI Pipeline is completed successfully 
#     steps: 
#       - name: Deploy to EC2
#         uses: appleboy/ssh-action@v1
#         with: 
#           host: ${{ secrects.SSH_ROOT }}
#           username: ${{ secrects.SSH_USERNAME}}
#           key: ${{ secrects.SSH_PRIVATE_KEY }} 
#           port: 22
#           script: |
#             echo "Start Deploying EC2"
#             cd /home/ubuntu
#             sudo bash -c 'echo "IMAGE_TAG=${{ github.event.workflow_run.head_sha }}" > .env'
#             sudo docker compose pull bookcar-backend
#             sudo docker compose up -d --force-recreate bookcar-backend
#             sudo docker image prune -f 
#             echo "Deploy Backend EC2 Successfully"

        
#   name: CD Pipleline
# runs-name: ${{ github.actor }}
# on:
#   workflow_run: 
#     workflows:
#       - CI Pipeline # Triggered when CI Pipeline is completed 
#     types: 
#       - completed 
#     branches: ['main','master']

# jobs: 
#   deploy-backend: 
#     name: Deploy Backend 
#     runs-on: ubuntu-latest
#     if: ${{ github.event.workflow_run.conclusion == 'success' }} # Only runs if CI Pipeline is completed successfully 
#     steps: 
#       - name: Deploy to EC2
#         uses: appleboy/ssh-action@v1
#         with: 
#           host: ${{ secrects.SSH_ROOT }}
#           username: ${{ secrects.SSH_USERNAME}}
#           key: ${{ secrects.SSH_PRIVATE_KEY }} 
#           port: 22
#           script: |
#             echo "Start Deploying EC2"
#             cd /home/ubuntu
#             sudo bash -c 'echo "IMAGE_TAG=${{ github.event.workflow_run.head_sha }}" > .env'
#             sudo docker compose pull bookcar-backend
#             sudo docker compose up -d --force-recreate bookcar-backend
#             sudo docker image prune -f 
#             echo "Deploy Backend EC2 Successfully"

        
#   deploy-frontend:
#     name: Deploy Frontend
#     runs-on: ubuntu-latest
#     if: ${{ github.event.workflow_run.conclusion == 'success' }}

#     steps:
#       - name: Trigger Vercel Deployment
#         run: |
#           curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
    
    