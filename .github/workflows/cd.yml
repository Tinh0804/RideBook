name: Legacy CD (disabled)

# Legacy workflow retained temporarily so existing GitHub references remain valid.
# Production releases are owned exclusively by release-backend.yml and
# deploy-production.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  migration-notice:
    name: Legacy CD is disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain the supported release path
        run: |
          echo "Legacy CD is disabled."
          echo "Run the Release Backend workflow to deploy an immutable image digest."
